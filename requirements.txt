python-dotenv>=1.0.0

# Required for the custom local setup and environment config.
# The app talks to Ollama/OpenAI-compatible endpoints over HTTP,
# so it does not require the official SDKs to run.

# Optional SDKs you may install if you want to use them explicitly:
# Official MCP SDK (Python 3.10+ only): mcp>=1.2.0
# Ollama Python SDK: ollama>=0.3.0
# Google GenAI SDK: google-genai>=1.0.0

# Testing and validation tools
pytest>=8.0.0
